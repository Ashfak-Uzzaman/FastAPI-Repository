# __init__.py is a special file used to mark a directory as a regular Python package, 
# enabling its modules to be imported into other parts of code. 
# When you import a package or its submodules, Python implicitly executes the code inside that package's __init__.py file exactly once.
# In modern python we can avoid to use it.